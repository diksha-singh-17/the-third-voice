import type { Scenario } from '../types';
const icons = ['↗','◷','✦'];
export function ScenarioPicker({scenarios,onStart,onCreate}:{scenarios:Scenario[];onStart:(s:Scenario)=>void;onCreate:()=>void}) { return <main className="landing"><div className="eyebrow">CONVERSATION INTELLIGENCE</div><h1>Make room for<br/><em>better conversations.</em></h1><p className="intro">The Third Voice listens quietly to workplace conversations—stepping in only when a gentle reframe can help.</p><div className="scenario-grid">{scenarios.map((s,i)=><article className="scenario-card" key={s.id}><div className="scenario-icon">{icons[i]}</div><span>SCENARIO 0{i+1}</span><h2>{s.scenario_title.replace('The ','')}</h2><p>{s.description}</p><div className="people">{s.participants.map(p=><div key={p.name}><b>{p.avatar_initial}</b>{p.name}<small>{p.role}</small></div>)}</div><button onClick={()=>onStart(s)}>Start demo <i>→</i></button></article>)}<article className="scenario-card custom-card"><div className="scenario-icon">＋</div><span>YOUR SITUATION</span><h2>Create your own</h2><p>Describe a real workplace conversation and let the Third Voice structure a fair, editable scenario.</p><div className="custom-mark">✦ Made for your context</div><button onClick={onCreate}>Create scenario <i>→</i></button></article></div></main> }
