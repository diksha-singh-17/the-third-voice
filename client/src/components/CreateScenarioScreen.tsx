import { useState } from 'react'; import type { Scenario } from '../types';
export function CreateScenarioScreen({onBack,onGenerated}:{onBack:()=>void;onGenerated:(scenario:Scenario, description:string)=>void}) { const [description,setDescription]=useState(''); const [loading,setLoading]=useState(false); const [error,setError]=useState(''); const generate=async()=>{setLoading(true);setError('');try{const response=await fetch('/api/scenarios/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({description})});const data=await response.json();if(!response.ok)throw new Error(data.error);onGenerated({...data,id:`custom-${crypto.randomUUID()}`,description},description)}catch(e){setError(e instanceof Error?e.message:'Something went wrong.')}finally{setLoading(false)}};return <main className="page"><header className="topbar"><button className="back" onClick={onBack}>← Scenarios</button><div className="brand">THE THIRD <b>VOICE</b></div><span/></header><section className="create"><div className="eyebrow">CREATE YOUR OWN SCENARIO</div><h1>Start with what’s <em>actually happening.</em></h1><p>Write the situation in your own words. The Third Voice will turn it into a balanced, editable conversation setup.</p><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe a workplace situation in your own words — e.g. ‘My teammate keeps missing standup because she says she’s overwhelmed with onboarding the new intern, but I think she’s also just distracted lately.’"/><div className="create-footer"><small>{description.length}/20 minimum characters</small><button className="primary" disabled={description.trim().length<20||loading} onClick={generate}>{loading?'Structuring your scenario…':'Generate scenario'} <i>{loading?'…':'→'}</i></button></div>{error&&<p className="form-error">{error}</p>}</section></main> }
