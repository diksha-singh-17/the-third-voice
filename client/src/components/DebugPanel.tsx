import type { Decision } from '../types'; export function DebugPanel({open,onClose,decisions}:{open:boolean;onClose:()=>void;decisions:Decision[]}) { return <aside className={'debug '+(open?'open':'')}><div className="debug-head"><div><span>JUDGE VIEW</span><h3>Decision log</h3></div><button onClick={onClose}>×</button></div><p className="debug-intro">Every turn is evaluated, including the moments the Third Voice stays quiet.</p>{decisions.length===0?<div className="empty">Waiting for the first message…</div>:<div className="decision-list">{[...decisions].reverse().map(d=><article key={d.turn}><div><b>TURN {d.turn} · {d.speaker}</b><em className={d.should_intervene?'yes':'no'}>{d.should_intervene?'INTERVENED':'SILENT'}</em></div><p>{d.reasoning}</p></article>)}</div>}</aside> }
